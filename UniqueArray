//去掉数组的重复元素
function Unique(Arr){
    if(Arr.length <= 1){
    	return Arr;
    }
    var o={},result=[];
    for(var i=0; i<Arr.length; i++){
    	var value = Arr[i];
        if(!o[value]){
            result.push(value);
            o[value] = 1;
        }
    }
    return result;
}
